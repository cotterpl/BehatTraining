{% extends "layout.twig" %}
{%  block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="page-header">
                <h1 id="tables">{{ movie.title }} ({{ movie.year }})</h1>
            </div>

            <div class="bs-component">
                <p>
                    {{ movie.description }}
                </p>
            </div>
        </div>
    </div>
{% endblock %}
